PREFIX4="128.130.95."
PREFIX6="2001:629:3200:95::1:"
FSINF_SUBNET4="128.130.95.0/25"
FSINF_SUBNET6="2001:629:3200:95::0/64"
FSINF_SUBNETS="${FSINF_SUBNET4} ${FSINF_SUBNET6}"

# firewall:
STYX4="${PREFIX4}10"
STYX6="${PREFIX6}10"
STYX="${STYX4} ${STYX6}"

# VM-server
SATURN4="${PREFIX4}11"
SATURN6="${PREFIX6}11"
SATURN="${SATURN4} ${SATURN6}"

# build-server
PALLENE4="${PREFIX4}13"
PALLENE6="${PREFIX6}13"
PALLENE="${PALLENE4} ${PALLENE6}"

# auth-server
QUAOAR4="${PREFIX4}14"
QUAOAR6="${PREFIX6}14"
QUAOAR="${QUAOAR4} ${QUAOAR6}"

# gateway
JANUS4="${PREFIX4}15"
JANUS6="${PREFIX6}15"
JANUS="${JANUS4} ${JANUS6}"

# Network infrastructure
ENCELADUS4="${PREFIX4}17"
ENCELADUS6="${PREFIX6}17"
ENCELADUS="${ENCELADUS4} ${ENCELADUS6}"

# VM-server
SOL4="${PREFIX4}18"
SOL6="${PREFIX6}18"
SOL="${SOL4} ${SOL6}"

# MySQL server:
MIMAS4="${PREFIX4}19"
MIMAS6="${PREFIX6}19"
MIMAS="${MIMAS4} ${MIMAS6}"

# mediawiki server:
TETHYS4="${PREFIX4}21"
TETHYS6="${PREFIX6}21"
TETHYS="${TETHYS4} ${TETHYS6}"

XOVER4="${PREFIX4}22"
XOVER6="${PREFIX6}22"
XOVER="${XOVER4} ${XOVER6}"

PLUTO4="${PREFIX4}23"
PLUTO6="${PREFIX6}23"
PLUTO="${PLUTO4} ${PLUTO6}"

# mail server
RHEA4="${PREFIX4}24"
RHEA6="${PREFIX6}24"
RHEA="${RHEA4} ${RHEA6}"

# irc.fsinf.at
FROST4="${PREFIX4}25"
FROST6="${PREFIX6}25"
FROST="${FROST4} ${FROST6}"

# drupal server
METHONE4="${PREFIX4}26"
METHONE6="${PREFIX6}26"
METHONE="${METHONE4} ${METHONE6}"

# webmail server
ATLAS4="${PREFIX4}27"
ATLAS6="${PREFIX6}27"
ATLAS="${ATLAS4} ${ATLAS6}"

# davcal
DAV4="${PREFIX4}29"
DAV6="${PREFIX6}29"
DAV="${DAV4} ${DAV6}"

# static HTML
SKATHI4="${PREFIX4}30"
SKATHI6="${PREFIX6}30"
SKATHI="${SKATHI4} ${SKATHI6}"

SKOLL4="${PREFIX4}31"
SKOLL6="${PREFIX6}31"
SKOLL="${SKOLL4} ${SKOLL6}"

ASK4="${PREFIX4}39"
ASK6="${PREFIX6}39"
ASK="${ASK4} ${ASK6}"

# monique (printer)
MONIQUE4="${PREFIX4}40"
MONIQUE6="${PREFIX6}40"
MONIQUE="${MONIQUE4} ${MONIQUE6}"

# signanz (informatik-forum.at)
SIGNANZ4="${PREFIX4}51"
SIGNANZ6="${PREFIX6}51"
SIGNANZ="${SIGNANZ4} ${SIGNANZ6}"

# iswjestija
ISWJESTIJA4="${PREFIX4}52"
ISWJESTIJA6="${PREFIX6}52"
ISWJESTIJA="${ISWJESTIJA4} ${ISWJESTIJA6}"

# makemake (minecraft)
MAKEMAKE4="${PREFIX4}61"
MAKEMAKE6="${PREFIX6}61"
MAKEMAKE="${MAKEMAKE4} ${MAKEMAKE6}"

# opossum (bl.mud.at)
OPOSSUM4="${PREFIX4}62"
OPOSSUM6="${PREFIX6}62"
OPOSSUM="${OPOSSUM4} ${OPOSSUM6}"

# titan (jabber.at)
TITAN4="${PREFIX4}63"
TITAN6="${PREFIX6}63"
TITAN="${TITAN4} ${TITAN6}"

# hyperion (jabber.at aux)
HYPERION4="${PREFIX4}64"
HYPERION6="${PREFIX6}64"
HYPERION="${HYPERION4} ${HYPERION6}"

# grace (netgrrrls.at)
GRACE4="${PREFIX4}65"
GRACE6="${PREFIX6}65"
GRACE="${GRACE4} ${GRACE6}"

# techtrrrs.at
TECHTRRRS4="${PREFIX4}67"
TECHTRRRS6="${PREFIX6}67"
TECHTRRRS="${TECHTRRRS4} ${TECHTRRRS6}"

# banzai (FeedHQ)
BANZAI4="${PREFIX4}71"
BANZAI6="${PREFIX6}71"
BANZAI="${BANZAI4} ${BANZAI6}"

WELCOMETUCODE4="${PREFIX4}72"
WELCOMETUCODE6="${PREFIX6}72"
WELCOMETUCODE="${WELCOMETUCODE4} ${WELCOMETUCODE6}"

# manni (backup server)
MANNI4="128.131.167.246"
MANNI="${MANNI4}"
